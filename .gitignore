node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Runtime data — keep dirs (.gitkeep) but ignore actual artifacts
.memory/*.db
.memory/*.db-*
.memory/raw/
.vectors/*.sqlite
.vectors/*.sqlite-*

# Tooling caches
.vitest/
.stryker-tmp/
reports/
coverage/
