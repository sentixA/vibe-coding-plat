# Subagent 共享 brief（M2-M8 必读）

## 0. 元约束（违反即返工）

- **只动你模块独占的文件**。详见下表。除了在根 `package.json` 增加你自己模块需要的 npm 脚本（如果原来是占位 stub），**不得改动根 package.json 的其他字段**，**不得改 `tsconfig.json` / `pnpm-workspace.yaml` / `AGENTS.md` / `CLAUDE.md` / 其它模块的源文件**。
- 必须是 ES module（package.json 已 `"type": "module"`），TS 通过 `tsx` 直跑，无需编译。
- 共享路径常量从 `scripts/_shared/paths.ts` 导入（相对导入，例：从 `scripts/memory.ts` 写 `import { MEMORY_DB } from './_shared/paths.js'`；从 `packages/memory/src/foo.ts` 写 `import { MEMORY_DB } from '../../../scripts/_shared/paths.js'`）。共享 SQLite 句柄从 `scripts/_shared/db.ts` 取（`openSqlite(absPath)` 异步），禁止自己 `new Database()`。**注意**：ESM 模式下 import 路径必须带 `.js` 扩展名（指向源 `.ts`，由 tsx 解析）。
- 你的 npm 依赖**只能加在自己 `packages/<module>/package.json`** 里，不要污染根 devDeps（除非确实是测试框架共享）。
- CLI 用 `cac`，统一支持 `--json` 输出。
- 中文注释，中文 commit 信息（如果你 commit），结尾"喵"。

## 1. 仓库语境（一句话）

`vibe-coding-plat` 是给 Claude Code 用的 vibe coding 工作流脚手架：spec-kit + chaos test + 代码向量检索 + 会话历史归档。技术栈：Node 22 + pnpm + TS + SQLite + sqlite-vss + vitest。

## 2. 完整计划

请先读 `/home/node/.claude/plans/mossy-gliding-ember.md` —— 那是定稿计划，包含 schema、目录结构、验证步骤。

## 3. 模块边界一览（§9.2 摘录）

| ID | 模块 | 独占文件/目录 | 对外暴露的脚本入口 |
|---|---|---|---|
| M2 | 记忆系统 | `scripts/memory.ts` `scripts/memory-ingest.ts` `packages/memory/**` `tests/unit/memory.test.ts` | `pnpm run memory <sub>` `pnpm run memory:ingest` |
| M3 | 代码向量索引 | `scripts/index.ts` `scripts/search.ts` `packages/vectors/**` `tests/unit/vectors.test.ts` | `pnpm run index` `pnpm run search` |
| M4 | spec-kit + wiki | `scripts/wiki-compile.ts` `packages/wiki/**` `.specify/constitution.md` `.wiki/index.md` `.wiki/log.md` | `pnpm run wiki:compile <slug>` |
| M5 | 测试闸门 | `scripts/verify.ts` `tests/chaos/**` `tests/mutation/**` `packages/chaos/**` `commitlint.config.cjs` | `pnpm run verify` `pnpm run test:chaos` `pnpm run test:mutation` |
| M6 | 沙箱+审批 | `scripts/sandbox-run.ts` `scripts/approve.ts` `.sandbox/bwrap.profile` `.sandbox/Dockerfile.dev` `packages/sandbox/**` | `pnpm run sandbox -- <cmd>` `pnpm run approve <plan\|diff\|commit>` |
| M7 | Git 工作流 | `.githooks/commit-msg` `.github/PULL_REQUEST_TEMPLATE.md` `packages/git/**` + 在根 `package.json` 加 `simple-git-hooks` 配置 | hook 自动触发 |
| M8 | 上下文注入 | `scripts/context.ts` `packages/context/**` `tests/unit/context.test.ts` | `pnpm run context <db-schema\|env\|api\|recent-logs>` |

## 4. 接口契约（§9.3 全文）

- 共享常量从 `scripts/_shared/paths.ts` 导入：`MEMORY_DB`, `VECTORS_DB`, `SPECIFY_DIR`, `WIKI_DIR`, `SANDBOX_DIR`, `CLAUDE_PROJECTS_DIR` 等（用相对路径 + `.js` 扩展名）。
- 共享 SQLite 句柄：`openSqlite(absPath)` 来自 `scripts/_shared/db.ts`，`const db = await openSqlite(MEMORY_DB)`。
- 日志：`log` / `jsonOut` 来自 `scripts/_shared/log.ts`。
- CLI 用 `cac`：每个脚本入口注册子命令，`--json` 切换 JSON 输出。
- 跨模块依赖通过 workspace 包：`@vcp/<module>`（package.json `"name": "@vcp/memory"` 等）。
- 工作区根已配 `pnpm-workspace.yaml: packages/*`，**直接在 packages/ 下创建你的子包**即可。

## 5. 子包模板

每个模块在 `packages/<module>/` 下建：

```
packages/<module>/
├── package.json     # name: @vcp/<module>, type: module, main: src/index.ts
├── src/
│   └── index.ts     # 库代码
└── README.md        # 一句话介绍
```

`scripts/<entry>.ts` 是 CLI 包装层，import `@vcp/<module>` 提供的库函数。

## 6. 环境注意

- **bwrap 缺失**：当前容器没装 bubblewrap。M6 必须有 Docker fallback **或** 在 bwrap 不在时降级为 dry-run + 警告，单元测试用 dry-run 模式。
- **better-sqlite3** 装得动（已验证 native build OK）。
- **网络**：可访问 npm registry。embedding 模型（`@xenova/transformers`）首次拉模型可能慢，可改为 mock 测试。

## 7. 完工标准（你必须自验）

- 你的代码通过 `pnpm -F @vcp/<module> exec vitest run`（或在根 `pnpm run test:unit`）。
- 你的 CLI 入口能 `pnpm run <entry> --help` 不报错。
- 单元测试覆盖你的核心路径。
- 不破坏任何其他模块的占位 stub（即 `pnpm run verify`、`pnpm run memory` 等其它模块的 stub 仍能输出 TODO）。

## 8. 报告格式

完工后用一段中文 ≤300 字汇报：
1. 你建了哪些文件
2. 你装了哪些 npm 包
3. 单元测试跑过几个用例
4. 任何 stub / dry-run / 待二期的功能
5. 任何**真实存在的**接口冲突或对其他模块的依赖
